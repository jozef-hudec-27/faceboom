<div class="home-page-wrapper">
    <div class="posts">
        <%= render 'post_create_card' %>

        <% @posts.each do |post| %>
            <%= render 'post', post: post %>
        <% end %>

        <%= paginate @posts, window: 2 %>
    </div>

    <div></div>
</div>
