<% if @post.saved_by? current_user %>
    <%= turbo_stream.update("post-#{@post.id}-save-wrapper", '<i class="fa-solid fa-bookmark"></i> Unsave') %>
<% else %>
    <%= turbo_stream.update("post-#{@post.id}-save-wrapper", '<i class="fa-regular fa-bookmark"></i> Save') %>
<% end %>
